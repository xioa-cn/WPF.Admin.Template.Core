<UserControl
    x:Class="WPFAdmin.Views.NotifyIconView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i18n="clr-namespace:WPF.Admin.Themes.I18n;assembly=WPF.Admin.Themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPFAdmin.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WPFAdmin.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:NotifyIconViewModel}"
    mc:Ignorable="d">
    <Border Width="300" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <Grid
                Grid.Row="0"
                Margin="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <RadioButton
                        Grid.Row="1"
                        Margin="10"
                        HorizontalAlignment="Left"
                        Content="{i18n:Localize Logout}"
                        IsChecked="{Binding Logout}">
                    </RadioButton>
                    <RadioButton
                        Grid.Row="0"
                        Margin="10"
                        Content="{i18n:Localize MiniIcon}"
                        HorizontalAlignment="Left"
                        IsChecked="{Binding Mini}">
                    </RadioButton>
                    <RadioButton
                        Grid.Row="2"
                        Margin="10"
                        Content="{i18n:Localize Close}"
                        HorizontalAlignment="Left"
                        IsChecked="{Binding Close}">
                    </RadioButton>
                </StackPanel>
            </Grid>
            <StackPanel
                Grid.Row="1"
                Margin="20"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Orientation="Horizontal">
                <Button
                    Margin="10,10,0,0"
                    Content="{i18n:Localize btn.Confirm}"
                    Command="{Binding ClosedCommand}"
                    Style="{StaticResource ButtonSuccess}">
                </Button>
                <Button
                    Margin="10,10,0,0"
                    Content="{i18n:Localize btn.Cancel}"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource ButtonInfo}">
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>