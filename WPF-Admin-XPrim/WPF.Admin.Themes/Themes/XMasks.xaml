<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:xi="clr-namespace:WPF.Admin.Themes">
    <Style x:Key="Load" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Grid>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <StackPanel.Resources>
                                <Style x:Key="Ellipse" TargetType="Ellipse">
                                    <Setter Property="Width" Value="20" />
                                    <Setter Property="Height" Value="20" />
                                    <Setter Property="Canvas.Left" Value="42.5" />
                                    <Setter Property="Fill" Value="{DynamicResource Primary.Brush}" />
                                    <Setter Property="RenderTransformOrigin" Value="0.5,3.33" />
                                </Style>

                                <PowerEase
                                    x:Key="Powerease"
                                    EasingMode="EaseInOut"
                                    Power="3" />

                            </StackPanel.Resources>
                            <Canvas
                                Width="100"
                                Height="100"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                                <Canvas.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)">
                                                <DoubleAnimation
                                                    AccelerationRatio="0.1"
                                                    DecelerationRatio="0.9"
                                                    EasingFunction="{StaticResource Powerease}"
                                                    Storyboard.TargetName="e1"
                                                    To="360"
                                                    Duration="0:0:2" />
                                                <DoubleAnimation
                                                    AccelerationRatio="0.3"
                                                    DecelerationRatio="0.7"
                                                    EasingFunction="{StaticResource Powerease}"
                                                    Storyboard.TargetName="e2"
                                                    To="360"
                                                    Duration="0:0:2" />
                                                <DoubleAnimation
                                                    AccelerationRatio="0.5"
                                                    DecelerationRatio="0.5"
                                                    EasingFunction="{StaticResource Powerease}"
                                                    Storyboard.TargetName="e3"
                                                    To="360"
                                                    Duration="0:0:2" />
                                                <DoubleAnimation
                                                    AccelerationRatio="0.7"
                                                    DecelerationRatio="0.3"
                                                    EasingFunction="{StaticResource Powerease}"
                                                    Storyboard.TargetName="e4"
                                                    To="360"
                                                    Duration="0:0:2" />
                                                <DoubleAnimation
                                                    AccelerationRatio="0.9"
                                                    DecelerationRatio="0.1"
                                                    EasingFunction="{StaticResource Powerease}"
                                                    Storyboard.TargetName="e5"
                                                    To="360"
                                                    Duration="0:0:2" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Canvas.Triggers>

                                <Ellipse Name="e1" Style="{StaticResource Ellipse}">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="0" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                                <Ellipse Name="e2" Style="{StaticResource Ellipse}">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="0" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                                <Ellipse Name="e3" Style="{StaticResource Ellipse}">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="0" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                                <Ellipse Name="e4" Style="{StaticResource Ellipse}">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="0" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                                <Ellipse Name="e5" Style="{StaticResource Ellipse}">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="0" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Canvas>
                        </StackPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Loading" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Grid Background="Transparent">
                        <ContentControl Style="{DynamicResource Load}" />
                        <TextBlock
                            Margin="0,20"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            FontFamily="Times New Roman"
                            FontSize="20"
                            FontWeight="Bold"
                            Foreground="#6b48ff"
                            Text="{TemplateBinding Tag}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type xi:XioaMasks}">
        <Setter Property="BackgroundOpacity" Value="0.8" />
        <Setter Property="Background" Value="#909399" />
        <Setter Property="Loadings" Value="Loading.." />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type xi:XioaMasks}">
                    <Grid>
                        <Grid
                            x:Name="MasksBorder"
                            Background="{TemplateBinding Background}"
                            Opacity="{TemplateBinding BackgroundOpacity}">
                            <Grid.Effect>
                                <BlurEffect Radius="0.5" />
                            </Grid.Effect>
                        </Grid>
                        <ContentControl Style="{StaticResource Loading}" Tag="{TemplateBinding Loadings}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>